import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Driver OS v2 - AI Quick Wins Assessment",
  description: "AI-powered assessment tool for construction companies to identify quick wins and operational improvements",
  keywords: ["construction", "AI assessment", "quick wins", "operational efficiency"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        {children}
      </body>
    </html>
  );
}
